FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml config.yaml ./
COPY shared/ shared/
COPY agents/bundle_validator/ agents/bundle_validator/

RUN pip install --no-cache-dir .

EXPOSE 8000

CMD ["uvicorn", "agents.bundle_validator.server:app", "--host", "0.0.0.0", "--port", "8000"]
