# Secrets only -- non-secret config lives in config.yaml

# LlamaStack API (not-needed for anonymous access)
LLAMASTACK_API_KEY=not-needed

# Neo4j password (cluster default)
NEO4J_PASSWORD=skillsmarketplace

# Local development overrides (uncomment as needed)
# NEO4J_URI=bolt://localhost:7687
# LLM_API_BASE=https://llamastack-llamastack.apps.ocp.v7hjl.sandbox2288.opentlc.com/v1
# OCI_REGISTRY_URL=https://default-route-openshift-image-registry.apps.ocp.v7hjl.sandbox2288.opentlc.com
