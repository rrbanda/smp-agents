apiVersion: v1
kind: Service
metadata:
  name: playground
  namespace: smp-agents
  labels:
    app.kubernetes.io/name: playground
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8000
      protocol: TCP
  selector:
    app.kubernetes.io/name: playground
    kagenti.io/type: agent
