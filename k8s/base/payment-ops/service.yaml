apiVersion: v1
kind: Service
metadata:
  name: payment-ops
  namespace: smp-agents
  labels:
    app.kubernetes.io/name: payment-ops
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8000
      protocol: TCP
  selector:
    app.kubernetes.io/name: payment-ops
    kagenti.io/type: agent
