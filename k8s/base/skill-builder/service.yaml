apiVersion: v1
kind: Service
metadata:
  name: skill-builder
  namespace: smp-agents
  labels:
    app.kubernetes.io/name: skill-builder
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8000
      protocol: TCP
  selector:
    app.kubernetes.io/name: skill-builder
    kagenti.io/type: agent
