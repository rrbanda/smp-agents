apiVersion: v1
kind: Service
metadata:
  name: kg-qa
  namespace: smp-agents
  labels:
    app.kubernetes.io/name: kg-qa
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8000
      protocol: TCP
  selector:
    app.kubernetes.io/name: kg-qa
    kagenti.io/type: agent
